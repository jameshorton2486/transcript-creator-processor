
/**
 * Placeholder Google API integration module
 * This is a temporary file to fix build errors while the actual implementation is in progress
 */

// Re-export any types or interfaces needed by other components
export interface GoogleTranscriptionOptions {
  // Add the properties expected by the components that use this
  diarize?: boolean;
  paragraphs?: boolean;
  formatParagraphs?: boolean;
  formatNames?: boolean;
  // Add any other required properties
}

// Add any other exports needed
